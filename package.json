{
  "name": "dandylow",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "parcel build src/index.html",
    "clean": "rm -rf dist .parcel-cache"
  },
  "devDependencies": {
    "parcel": "^2.12.0"
  },
  "dependencies": {
    "pitchy": "^4.1.0",
    "vexflow": "^5.0.0"
  }
}
